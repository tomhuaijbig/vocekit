# 2026-06-26 快捷键展示拆分

## 改动

- 新增 `src/ui/shortcut_display.h/.cpp`。
- 把快捷键展示格式化从 `voiceassistant.cpp` 拆出。

## 目的

- 首页、设置页、功能自定义页都可以复用同一套快捷键显示规则。
- 后续如果要把 `Alt+X` 改成更漂亮的标签或本地化文案，只需要改一个模块。

## 验证重点

- 首页功能卡片、设置快捷键页、功能自定义页里的快捷键仍显示为 `Alt + X` 这类格式。
- 空快捷键仍显示为“未设置”。

# 2026-06-26 统一提示弹窗模块抽离

## 本次调整

- 新增 `src/ui/attention_message.h/.cpp`，集中处理提示弹窗。
- 从 `voiceassistant.cpp` 移出：
  - 弹窗常见问题编号匹配。
  - “查看解决办法”回调。
  - 弹窗一次置前、居中、按钮中文化等 UI 行为。
- 主程序通过 `setAttentionFaqCallback()` 注册常见问题跳转，不再持有弹窗内部状态。

## 保留行为

- 报错弹窗继续显示普通数字编号。
- 弹窗继续提供“查看解决办法”按钮。
- Windows 下仍然只短暂置前一次，不使用长期置顶。
- 常见问题页继续使用同一套编号逻辑，避免弹窗编号和页面搜索编号不一致。

## 验证

- 使用 Qt 5.9 MinGW release 构建通过。

# vocekit 测试说明

## 开始使用

1. 解压整个压缩包，不要只单独运行程序文件。
2. 双击 `vocekit.exe`。程序启动后会在系统托盘后台运行，并打开主界面。
3. 首次使用请打开“设置 -> 接口”，填写测试人员自己的接口密钥。
4. 在“设置 -> 接口”选择百度语音识别或讯飞语音听写。
5. 在“设置 -> 快捷键”查看或修改各功能快捷键。

测试包不包含开发者的接口密钥、录音和历史记录。没有填写接口密钥时，对应功能会提示配置不完整。

Windows 可能因为程序暂未签名而显示安全提醒。请确认压缩包来源可信后，再选择继续运行。

## 建议测试内容

- 程序关闭主窗口后是否仍在托盘运行，托盘菜单能否正常打开和退出。
- 听写、翻译、问答和自定义功能的快捷键是否正常。
- 翻译能否读取鼠标拖动选中的文字。
- 自动写入和结果小框两种展现方式是否正常。
- 结果小框的复制、写入、替换选中、关闭、重新生成、换模型重试、继续追问和展开全文是否正常。
- 大模型返回时是否能边生成边显示；网络差或代理异常时是否有清楚提示。
- 主页功能卡片的拖动排序是否能保存。
- 录音时浮动条波形是否会随麦克风声音变化。
- 录音前倒计时和提示音开关是否按设置生效。
- 在“功能自定义”中把某个语音功能改为“按住说话”，确认按下开始、松开结束，并且普通键盘输入不会进入日志。
- 开启长录音后连续录制 70 秒以上，确认录音不中断、浮动条显示当前段号，结束后能按顺序合并文字。
- 长录音结束后打开历史详情，确认完整录音可播放，每个分段都能单独播放或复制文字。
- 在历史详情里对失败分段点击“重试识别”，确认使用当前语音服务重试并更新记录。
- 模拟一次语音接口短暂失败，确认失败段只自动重试一次，其它成功段仍能合并。
- 如果所有分段都失败，确认不会调用大模型，并出现带常见问题编号的明确提示。
- 提示词编辑、锁定和各功能独立模型设置是否正常。
- 历史记录的加载、播放、复制、收藏和删除是否正常。
- 直连与使用系统代理两种网络方式是否正常。
- 历史记录保存位置能否修改，文本记录、录音记录、详细记录和总目录是否按功能与日期保存到所选位置。
- 左侧“测试诊断”里的接口自检、网络诊断、麦克风测试、浮动条测试和结果小框测试是否正常。
- 左侧“图片识别”能否选择 PNG、JPG、BMP 或 WebP，并在后台完成识别。
- “自动选择”在 RapidOCR 不可用时能否自动回退到 Windows OCR，且程序不会崩溃。
- 图片识别结果能否编辑、复制，并发送给智能整理、翻译、润色、总结或问答。
- 历史记录中是否出现“图片识别”标签，详情是否显示引擎、语言、耗时和回退状态。
- “测试工具 -> 接口自检 -> 当前图片识别接口”能否返回明确结果。
- 左侧“常见问题”是否能找到弹窗报错对应的解决办法。

### 功能流程画布专项

- 进入任一功能并点击“画布”，确认“放置、撤销、重做、缩放、适应窗口、应用流程、停用流程”均可见。
- 展开节点库，确认九种节点名称、端口和 Inspector 中文字段没有裁剪；分别检查恢复窗口、最大化窗口及 100%、125%、150% 缩放。
- 画布模式不应出现页面滚动条或画布内部滚动条；普通滚轮不得移动页面或画布，空白处按住鼠标左键拖动应平移视口，`Ctrl + 滚轮` 应围绕鼠标位置缩放。
- 编辑或平移后立即点击“返回设置”，草稿和视口必须先同步保存；保存失败时应留在画布，不能切回设置后再由延迟定时器重建页面。
- 在多个功能之间快速切换，读取失败时页面、控制器和编辑器必须继续保持原功能 ID，不能把后续编辑写入旧功能。
- 放置、拖动、连接、删除节点，确认拖动期间不弹 Inspector，松手后只产生一次撤销记录。
- 草稿自动保存、保存失败、可发布、发布失败和停用状态应清晰；编辑草稿不能改变正在运行的发布版本。
- 分别验证主快捷键、独立截图快捷键和截图悬浮入口。没有可用发布画像时走经典功能；配置错误、运行失败或取消后不能重复执行经典功能。
- 验证 `Selection -> Input -> Model -> Output -> ResultPopup`、语音流程、截图对照流程、双输入汇合、串联两个模型和一个结果进入多个输出动作。
- 自动写入的“替换”必须在原选区仍存在时才执行；选区丢失时提示兜底，不能退化成普通插入。
- 取消倒计时、录音、OCR、模型和结果生成，确认迟到结果不会改变节点状态或下一次运行。
- 一次流程即使有多个输出动作也只新增一条历史；历史保留发布版本、触发入口、节点耗时和安全元数据，不保存截图正文。

图片识别默认在本机处理。只有测试人员主动选择“自定义云 OCR”并在确认框中同意后，图片才会发送到填写的接口地址；历史记录不会复制或保存原图片。

## 反馈问题

反馈时请提供：

- 问题发生前执行的步骤。
- 实际结果和期望结果。
- 错误提示截图。
- Windows 版本以及使用的语音识别服务、模型服务。

请勿发送 `config/secrets.json`，该文件包含测试人员自行填写的接口密钥。

## 自动测试

运行全部测试工程：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File scripts\run-all-tests.ps1
```

脚本会严格枚举 `tests` 下扩展名为 `.pro` 的工程，构建并运行 QtTest 和独立冒烟程序，最后汇总通过、失败和跳过数量。测试生成的临时 `Makefile.codex*` 会自动清理。

功能流程专项工程覆盖：

- 图、端口、JSON、校验、编译、发布事务、计划缓存和串行调度。
- 编辑控制器、画布场景、节点、单个 Input 接收多条来源连线、连线 Delete 同步刷新安全性、节点库拖放全链路和中心定位、Inspector 展开视角稳定性、同 revision 视口回显、真实功能页固定工作区、反复刷新所有权、保存失败留页、滚轮隔离、空白左键平移、缩放、底部状态精简、紧凑宽度和清理。
- 选中文字、语音、截图、模型消息、模型任务、运行适配器、执行控制器和有界取消。
- 结果动作、历史 exactly-once、隐私日志、稳定错误码、FAQ、经典兜底和设置事件分流。

2026-07-28 的完整基线为 146 个测试工程，其中 144 个 QtTest、2 个独立冒烟程序，共 1114 项通过，`Failed`、`Skipped` 和 `InfrastructureFailures` 均为 0。

2026-07-29 的画布视觉与交互专项验收使用 Qt 5.9 / MinGW 5.3 Debug 构建：

- `function_canvas_visual_style_tests`：8 项通过。
- `function_canvas_view_tests`：22 项通过。
- `function_canvas_scene_tests`：19 项通过。
- `function_canvas_editor_tests`：31 项通过。
- 图、校验、编译、编辑控制器和发布服务五个领域/控制器工程：78 项通过。
- 本轮聚焦回归合计 158 项通过，0 项失败、0 项跳过；主程序 Debug 完整链接成功。
- `QT_SCALE_FACTOR=1.5` 下单独复跑 Inspector 分组、中文字体、长文本、焦点和隐藏状态用例，7 个目标用例及初始化/清理均通过。

真实 Windows 界面还应复核以下交互；2026-07-29 已在系统 150% 缩放下完成一次最大化与紧凑窗口验收：

- 35%、100% 和约 200% 高倍率下，从节点库拖入节点时，卡片中心应落在鼠标松开位置附近。
- “语音采集”和“选中文字”应能同时连接同一个“输入节点”的“文字输入”端口。
- 选中连线后按 Delete，只删除该连线，程序继续运行；输入框内 Delete/Backspace 只编辑文字。
- 空白左键拖动只平移画布；普通滚轮不平移画布；节点库、Inspector、工具栏和底部状态始终留在视口坐标系。
- 紧凑宽度只显示最后打开的一个侧栏；宽窗口允许节点库与 Inspector 同时显示。

画布紧凑布局还应在三个缩放因子下运行：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
$env:QT_AUTO_SCREEN_SCALE_FACTOR = "0"
foreach ($scale in "1", "1.25", "1.5") {
  $env:QT_SCALE_FACTOR = $scale
  .\tests\ui\debug\function_canvas_editor_tests.exe `
    compactEditorKeepsToolbarAndFooterControlsInside -maxwarnings 0
}
```

功能配置和结果流程使用可执行的 Qt 组件测试，不再通过 PowerShell 搜索旧主文件中的字符串：

- `hub_settings_state_tests` 验证逐功能录音参数、结果操作和持久化。
- `result_flow_tests` 验证网络策略、流式失败回退和现场恢复。
- `result_choice_popup_header_tests` 验证结果按钮配置、透明度、完成回调和取消。

录音状态机测试：

```powershell
.\build-tests\recording-red\debug\recording_core_tests.exe -txt
```

当前覆盖默认配置、范围约束、按住快捷键状态、自动重复过滤、分段顺序、单次重试、成功段计数、33 段上限和 WAV PCM 提取。

生成最终测试包：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File scripts\package-test.ps1 `
  -PackageName vocekit-canvas-test
```

生成后必须同时检查目录和 ZIP：不得包含真实 `secrets.json`、真实 settings/prompts、日志、历史、录音、截图、源码、对象文件、开发者绝对路径或 Git 文件；包内 `secrets.json` 只能来自空白示例，运行库检查必须再次通过。
